name: "CodeQL Config"

queries:
  - uses: security-extended
  - uses: security-and-quality

paths:
  - html/lib/jquery/js

paths-ignore:
  - '**/node_modules'
  - '**/dist'
  - '**/build'

query-filters:
  - include:
      tags contain: security
      tags contain: external/cwe/cwe-116 